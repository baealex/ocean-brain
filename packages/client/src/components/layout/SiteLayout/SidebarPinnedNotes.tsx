import PinnedNotesPanel from './PinnedNotesPanel';

const SidebarPinnedNotes = () => (
    <section className="p-3 pt-0" aria-label="Pinned notes">
        <PinnedNotesPanel />
    </section>
);

export default SidebarPinnedNotes;
